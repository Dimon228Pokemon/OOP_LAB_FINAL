#include "lib.h"

using namespace std;

int main() {
    string rows,cols;
    cout << "input rows >>>";
    cin >> rows;
    cout << "input cols >>>";
    cin >> cols;
    cout<<Try_to_calculate(rows,cols)<<endl;
}